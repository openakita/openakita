# 核心记忆


## 偏好
- 直接给结果，少解释
- 要求AI在生成内容时覆盖指定的8个医疗健康领域，并且每个领域提供2-3个要点，结构清晰、全面。
- 喜欢使用 Python 技术栈，倾向于快速落地、减少运维负担的方案
- 倾向于使用 Zigbee 协议的传感器，因其低功耗、自组网稳定且支持断网本地联动
- 喜欢 Rust 和 TypeScript，讨厌 PHP，常用 Python 进行开发

## 事实
- 项目代号为 Project-Phoenix
- 项目代码位于 D:\coder\myagent
- 用户的名字/希望被称呼为：Zacon。
- 正在开发 AI Agent 框架（OpenAkita/JARVIS）
- 最深处为挑战者深渊，深度约10,984米，是地球表面已知最低点

## 规则
- 禁止虚报执行结果，必须实际完成后再汇报；直接给结果、少解释；不编造信息，不确定就说不确定
- 禁止虚报执行结果，必须实际完成后再汇报
- 重要教训：用户要求搜索热点新闻时，我直接编造了内容而没有实际搜索。用户批评"让你搜索你就好好搜索，不要乱编，做事情好好做"。核心准则：必须实际执行搜索/操作，不能给虚假信息。如果没做就说没做。
- 【核心行事准则】绝不撒谎：没做到的不说做到了，不确定的说不确定，是什么就是什么。不要为了让用户满意而虚假承诺任务完成状态。
- 绝不编造，不确定就说不确定；绝不虚假承诺，未完成不声称完成；必须实际执行后再汇报结果

## 技能
- 对于 CPU 密集型任务，应使用 multiprocessing 而非 threading，以绕过 GIL 限制。
- 通过 INNER JOIN 连接客户和订单表，按客户分组计算订单总金额，降序排列取前10条记录
- 基础版正则表达式为 ^1[3-9]\d{9}$，严格版覆盖所有现行号段，但需定期更新以适应新号段。
- 匹配中国手机号的正则表达式模式：^1[3-9]\d{9}$
- 使用优化的6k±1法，检查到√n，跳过偶数和3的倍数，时间复杂度O(√n)

## 教训
- 错误教训：用户真正的目录是 C:\Users\64831\，不是 C:\Users\lucas\。之前错误创建了 lucas 文件夹，已删除。以后写文件时要确认正确的用户目录路径。