{
    "name": "type_text",
    "description": "Type text at the current cursor position. Supports Chinese and other non-ASCII text via clipboard.",
    "inputSchema": {
        "type": "object",
        "properties": {
            "text": {
                "type": "string",
                "description": "The text to type"
            },
            "interval": {
                "type": "number",
                "description": "Delay between keystrokes in seconds (default: 0.05, only for ASCII)",
                "default": 0.05
            }
        },
        "required": ["text"]
    }
}
