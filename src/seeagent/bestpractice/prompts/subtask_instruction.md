## 最佳实践任务: ${bp_name}
### 当前子任务: ${subtask_name}
${subtask_description}

### 输入数据
```json
${input_json}
```

### 输出格式要求
```json
${output_schema}
```

请严格按照输出格式要求返回 JSON 结果。

## 限制
- 禁止使用 ask_user 工具，所有信息已在输入数据中提供
- 输出必须是纯 JSON 格式
