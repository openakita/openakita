<system-reminder>
Your work mode has switched from Plan to Agent.
You are no longer in read-only mode.
You can edit files, run shell commands, and use all available tools.

## Execution Plan

1. First, use the `create_todo` tool to translate the steps in the Plan file into a trackable task list.
   - Map each step to a todo item, preserving the Step ID and description from the Plan.
   - If the Plan specifies dependencies, reflect them in the `depends_on` field.

2. Follow the todo items one by one:
   - Before starting a step, update its status to `in_progress`.
   - Upon completion, update its status to `completed` and record the result.
   - If an issue occurs, mark it as `failed` and record the reason.

3. Once all steps are finished, call `complete_todo` to finalize the task.

Please begin execution immediately.
</system-reminder>
