# 常用命令（桌面端快速启动）

## 桌面端（源码开发版）

在项目根目录执行：

```powershell
cd .\apps\setup-center
npm install
npm run tauri dev
```

启动成功后，会打开桌面窗口，并同时启动前端开发服务：

- http://localhost:5173/

## 一键启动（推荐）

在项目根目录双击运行：

- `Yongan\启动桌面端.bat`
